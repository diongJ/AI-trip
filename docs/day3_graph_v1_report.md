# Day 3 可靠知识图谱 V1 报告

## 完成情况

Day 3 使用 36 份官方资料的逐文档抽取结果，完成确定性实体融合、语义审核、统一图谱生成和 Neo4j Aura 实际入库。全程未增加模型扩写内容，也未为了达到早期数量目标补造实体或关系。

| 指标 | 融合前 | 图谱 V1 |
|---|---:|---:|
| 文档 | 36 | 36 |
| 实体出现次数 | 202 | — |
| 唯一实体 ID | 94 | 78 |
| 关系 | 89 | 87 |
| 来源或证据问题 | 0 | 0 |

统一图谱由 `python -m scripts.build_graph_v1` 可重复生成，默认输出到 `data/graph/knowledge_graph_v1.json`。融合统计保存在 `data/processed/graph_v1_fusion_report.json`。

## 人工消歧与审核

人工消歧配置位于 `data/curated/entity_resolution_v1.json`，是图谱可重复构建的版本化依据。

- 人物：将“南越王”“南越文帝”归一到“赵眜”。
- 墓葬：将“南越王墓”“赵眜墓”归一到“南越文王墓”。
- 朝代：将本语料语境中的“汉朝”“汉代”归一到“西汉”。
- 展览：将完整展览名称归一到“南越藏珍”。
- 文物：统一文帝行玺、错金铭文铜虎节、铜印花板模和犀角形玉杯的名称变体。
- 删除 DOC_011 中误识别为展览实体的四个章节名称。
- 删除 DOC_017 中仅以“南越文王墓出土”为证据推导的两条赵眜关系。

融合器只执行明确配置的决策，不使用字符串相似度自动合并。映射环、跨类型映射、缺失规范目标、残留关系和未命中的删除规则都会中止构建。

## Aura 入库验收

执行 `python -m scripts.load_graph_v1`，在不清空 Day 1 样例数据的前提下完成两次幂等写入。

| 阶段 | 节点 | 关系 |
|---|---:|---:|
| 写入前 | 6 | 6 |
| 第一次写入后 | 80 | 93 |
| 第二次写入后 | 80 | 93 |

- 第一次写入后缺失实体：0。
- 第一次写入后缺失关系：0。
- 第二次写入后缺失实体：0。
- 第二次写入后缺失关系：0。
- 第二次写入未新增节点或关系，幂等性通过。
- 已查询到赵眜与南越国、南越文王墓，文帝行玺与赵眜、材料、纹饰、朝代等核心路径。

数据库总量不是 `78 + 6`，因为 Day 1 样例中的部分规范实体与 V1 重合。完整机器验收结果保存在 `data/processed/graph_v1_load_report.json`。

## 测试与已知边界

语料校验、逐文档抽取审计、统一图谱构建和 36 项离线测试全部通过。

图谱 V1 以可靠性为优先，规模低于早期提出的 150～300 个实体、300～700 条关系。当前墓室和出土位置信息仍偏少，`TombChamber` 仅 1 个，`EXCAVATED_FROM` 仅 1 条，且官方语料尚未产生 `CONTAINS` 关系。Day 4 不应通过推断补齐这些关系。

## Day 4 输入

Day 4 应直接复用：

- `data/graph/knowledge_graph_v1.json`：KG 检索和 Neo4j 查询基准。
- `data/raw/**/*.json`：文档切分和 RAG 索引来源。
- `data/curated/entity_resolution_v1.json`：问题实体别名归一依据。
- Neo4j 关系的 `document_id` 和 `evidence`：回答引用与可追溯展示。
